ext {
  name = 'tasting'
}

def subproject(subname) {
  "${name}-${subname}"
}

rootProject.name = subproject('base')
rootProject.projectDir = new File(settingsDir, rootProject.name)
includeFlat subproject('common'), subproject('assertjdbe')
